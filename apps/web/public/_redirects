# Cloudflare Pages SPA Routing Configuration

# API proxy to backend (MUST be before SPA fallback)
/api/*  https://trendy-api-prod-541486728137.us-central1.run.app/api/:splat  200

# Serve index.html for all routes (SPA fallback)
# The 200 status ensures the URL stays the same in the browser
/*    /index.html   200
